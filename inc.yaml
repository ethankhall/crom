exec:
  ci:
    commands:
      - cargo build
      - cargo test
  build:
    commands:
      - cargo build
      - cargo fmt
      - cargo clippy
    description: Run a normal debug build
  format:
    commands:
      - find . -name '*.rs' -not -path '*target*' -exec rustfmt {} \;
